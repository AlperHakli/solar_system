import 'package:flutter/material.dart';

class Imagesliderlittledots extends StatelessWidget {
  const Imagesliderlittledots({super.key});

  @override
  Widget build(BuildContext context) {
return Center();
  }
}
