import 'package:flutter/material.dart';

class Aboutonespacething extends StatelessWidget {
  const Aboutonespacething({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [

        ],
      ),
    );
  }
}
